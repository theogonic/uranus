import { RemoveSuffixPipe } from './remove-suffix.pipe';

describe('RemoveSuffixPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveSuffixPipe();
    expect(pipe).toBeTruthy();
  });
});
